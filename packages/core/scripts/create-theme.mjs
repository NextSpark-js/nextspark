#!/usr/bin/env node
console.error('Themes are install-once project templates in root-first NextSpark. Create a project with the desired template instead of adding a theme directory.')
process.exit(1)
