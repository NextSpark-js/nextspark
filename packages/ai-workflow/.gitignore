# Synced directories (generated by scripts/sync.mjs from repo/.claude/)
# Run `pnpm sync` before npm publish to populate these.
claude/agents/
claude/commands/
claude/skills/
claude/templates/
claude/workflows/
claude/_docs/
claude/scripts/

# Synced schema files
claude/config/*.schema.json

# Session data (never synced)
claude/sessions/

# Keep config templates (consumer placeholders — NOT synced)
!claude/config/context.json
!claude/config/workspace.json
!claude/config/team.json
!claude/config/github.json
