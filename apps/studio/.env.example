# Required: Your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Override the Claude model (default: claude-sonnet-4-5-20250929)
# STUDIO_MODEL=claude-opus-4-6

# Required: PostgreSQL connection
DATABASE_URL=postgresql://user:pass@localhost:5432/studio

# Required: Better Auth secret (generate with: openssl rand -hex 32)
BETTER_AUTH_SECRET=your-secret-here

# Required: App URL (used by auth client)
NEXT_PUBLIC_APP_URL=http://localhost:4000

# Optional: Registration mode — "open" (default) or "invite" (first user only)
# STUDIO_REGISTRATION=invite
