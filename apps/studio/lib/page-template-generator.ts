/**
 * Re-export page template generator from @nextsparkjs/studio package.
 * This file exists for convenience — API routes can import from here or from the package.
 */
export { generatePageTemplate, generateAllPageTemplates, getTemplateFilePath } from '@nextsparkjs/studio'
